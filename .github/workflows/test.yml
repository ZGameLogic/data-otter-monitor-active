name: test

on:
  push:
jobs:
  disable-monitor:
    uses: ./action.yml
    with:
      monitorId: 1
      active: false
  enable-monitor:
    uses: ./action.yml
    needs: disable-monitor
    with:
      monitorId: 1
      active: true